- [ ] Left pane with control widget should be 20% of the window width by default.
- [ ] Rename "Name" frame of the control widget to "Widget". Add `width` and `height` rows with inputs and up/down buttons to adjust the values. Set default values to 500.
- [ ] Add `Escape` hotkey to the main window that closes it and exists the app.