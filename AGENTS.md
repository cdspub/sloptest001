# Development
Never add inline documentation.
Never add unit tests.
You are working on the `main` branch that may have external changes. Run `git fetch` and `git pull --rebase origin main` before processing any of the user's requests.